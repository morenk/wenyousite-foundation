# Flutter profile

- Android 8+ 手机竖屏优先，共享代码保持 iOS 可兼容；不复制桌面三栏与固定侧栏。
- 使用与 Web 相同的纯白画布、柔粉主色、深紫灰正文和三角色自托管字体。
- 页面保持单列；360–400dp 使用 12dp 外边距，401–599dp 使用 24dp，600dp 以上限制内容宽度。
- 所有可点击控件命中区不小于 48×48dp；保留系统安全区、文字缩放、软键盘滚动和系统返回。
- 卡片使用白底、细边框和圆角区分层次，不用粉色页面底或常驻阴影制造层级。
- 编辑器固定核心一级栏与原生“更多”底部面板，不把 PC 全量按钮压进手机宽度，也不横向滑动。
- 可交互图片进入全屏查看路由，保留系统返回、双击/捏合缩放与放大后平移；未放大时才允许下滑关闭。
- Material Icons Rounded 继续作为移动端图标实现；Web 图标库不构成跨端资产契约。
